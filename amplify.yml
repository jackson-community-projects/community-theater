version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npx ampx pipeline-deploy --branch "$AWS_BRANCH" --app-id "$AWS_APP_ID"
        - echo "ADMIN_BOOTSTRAP_SECRET=$ADMIN_BOOTSTRAP_SECRET" >> .env.production
        - echo "STAFF_SIGNUP_SECRET=$STAFF_SIGNUP_SECRET" >> .env.production
        - echo "TMDB_BEARER_TOKEN=$TMDB_BEARER_TOKEN" >> .env.production
        - echo "TMDB_API_READ_ACCESS_TOKEN=$TMDB_API_READ_ACCESS_TOKEN" >> .env.production
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
